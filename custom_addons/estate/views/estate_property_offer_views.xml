<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Property Offer Tree View -->
    <record id="estate_property_offer_view_tree" model="ir.ui.view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list editable="bottom" 
                  decoration-success="status == 'accepted'" 
                  decoration-danger="status == 'refused'">
                <field name="property_id"/>
                <field name="partner_id"/>
                <field name="price"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <field name="status" column_invisible="True" optional="False"/>
                <field name="property_type_id"/>
                <button name="action_accept" type="object" string="Accept" invisible="status in ('accepted', 'refused')"/>
                <button name="action_refuse" type="object" string="Refuse" invisible="status in ('accepted', 'refused')"/>
            </list>
        </field>
    </record>

    <!-- Property Offer Form View -->
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="property_id"/>
                        <field name="partner_id"/>
                        <field name="price"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="status"/>
                        <field name="property_type_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    
    <!-- Property Offer Action -->
    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
