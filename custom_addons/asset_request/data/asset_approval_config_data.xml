<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <!-- ============================================================ -->
        <!-- Pre-seeded Approval Configuration (3 levels, no approver yet) -->
        <!-- ============================================================ -->
        <record id="approval_config_level_1" model="asset.approval.config">
            <field name="level">1</field>
        </record>

        <record id="approval_config_level_2" model="asset.approval.config">
            <field name="level">2</field>
        </record>

        <record id="approval_config_level_3" model="asset.approval.config">
            <field name="level">3</field>
        </record>
    </data>
</odoo>
